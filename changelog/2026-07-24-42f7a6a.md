# 提交报告 - 2026-07-24

## 提交信息
- **Commit Hash**: 42f7a6a
- **消息**: feat: improve scan workflow usability

## 改动摘要
- 增加快速扫描预设：快速发现、Web 暴露面、深度探测。
- 增加配置自动保存与恢复默认配置，减少重复填写。
- 增加目标、端口、超时、并发参数校验，提前阻止无效扫描。
- 增加 `/` 聚焦搜索、`R` 刷新、`Esc` 清空搜索等快捷键。
- 增加结果刷新、复制当前 IP、复制单个 IP 与结果数量提示。
- 优化移动端预设与结果操作布局。

## 验证
- `node --check web/app.js`
- `git diff --check`
- 浏览器模拟 API 验证预设、表单校验、配置持久化、快捷键通过。

## 风险评估
**整体风险等级：低风险**

本次只修改 Web 前端交互，不改变后端 API 契约。
