# 提交报告 - 2026-07-24

## 提交信息
- **Commit Hash**: c30f6b1
- **消息**: feat: add versioned backend protocol and remote connection

## 改动摘要
- 新增 `/api/v1/system` 协议发现接口，返回服务版本、状态、能力和端点清单。
- 前端不再固定使用同源 API，支持输入任意兼容后端地址或通过 `?api=` 指定。
- 增加连接测试、协议版本校验、服务状态、能力数量和自动健康轮询。
- 增加前后端协议契约文档 `docs/API_CONTRACT.md`。

## 验证
- `cargo test --quiet`（30 passed，1 ignored）
- `cargo clippy --all-targets -- -D warnings`
- `cargo build --quiet`
- `node --check web/app.js`
- API-only 实测 `/api/v1/system` 与 OpenAPI 路径
- 浏览器模拟远程后端地址切换与协议发现

## 风险评估
**整体风险等级：中低风险**

新增版本化发现接口并保持既有资源接口不变；前端在协议不匹配或连接中断时显示明确状态，不继续把旧数据标记为实时。
